// place files you want to import through the `$lib` alias in this folder.

export interface ArtResponse {
	id: number;
	uuid: string;
	category: string;
	author: string;
	title: string;
	description: string;
}
